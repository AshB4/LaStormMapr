const Footer = () => (
  <footer className="bg-primary text-white text-center py-3 mt-auto">
    <p className="mb-0">&copy; {new Date().getFullYear()} LAStormMapr</p>
    <small className="d-block">Local storms. Local tools — stay safe with us.</small>
  </footer>
);

export default Footer;
